<%= form_with(model: page, local: true) do |form| %>
  <% if page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :page_user_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :page_title %>
  </div>

  <div class="field">
    <%= form.label :page_name %>
    <%= form.text_field :page_name, id: :page_page_name %>
  </div>

  <div class="field">
    <%= form.label :access_token %>
    <%= form.text_area :access_token, id: :page_access_token %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
